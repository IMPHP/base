# [I/O](io.md) / [NullStream](io-NullStream.md) :: DEF_MODE
 > im\io\NullStream
____

## Description
Default mode that is used when nothing else is defined.

## Synopsis
```php
public string DEF_MODE = 'r+'
```
