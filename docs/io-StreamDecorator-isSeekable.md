# [I/O](io.md) / [StreamDecorator](io-StreamDecorator.md) :: isSeekable
 > im\io\res\StreamDecorator
____

## Description
Check if the stream is seekable.

 > This is the same as `$this->getFlags() & static::F_SEEKABLE > 0`  

## Synopsis
```php
public isSeekable(): bool
```
